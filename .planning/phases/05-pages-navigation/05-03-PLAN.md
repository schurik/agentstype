---
phase: 05-pages-navigation
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/about/page.tsx
  - app/components/about/Bio.tsx
  - app/components/about/SocialLinks.tsx
autonomous: false

must_haves:
  truths:
    - "About page shows photo/avatar"
    - "About page shows full bio text"
    - "GitHub link is visible and clickable"
    - "X/Twitter link is visible and clickable"
    - "About page is mobile responsive"
  artifacts:
    - path: "app/about/page.tsx"
      provides: "About page with bio and social links"
      min_lines: 25
    - path: "app/components/about/Bio.tsx"
      provides: "Bio section with photo and long-form text"
      exports: ["Bio"]
      min_lines: 30
    - path: "app/components/about/SocialLinks.tsx"
      provides: "Social media links component"
      exports: ["SocialLinks"]
      min_lines: 15
  key_links:
    - from: "app/about/page.tsx"
      to: "app/components/about/Bio.tsx"
      via: "import and render"
      pattern: "import.*Bio"
---

<objective>
Create the About page with full bio, photo/avatar, and social links (GitHub, X/Twitter).

Purpose: Tell visitors who the developer is and provide ways to connect.
Output: Complete About page satisfying ABOUT-01, ABOUT-02, ABOUT-03 requirements.
</objective>

<execution_context>
@/Users/alexander.buss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexander.buss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-pages-navigation/05-CONTEXT.md
@.planning/phases/05-pages-navigation/05-RESEARCH.md
@.planning/phases/05-pages-navigation/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SocialLinks and Bio components</name>
  <files>
    app/components/about/SocialLinks.tsx
    app/components/about/Bio.tsx
  </files>
  <action>
1. Create `app/components/about/SocialLinks.tsx`:
   - Server Component (pure links, no state)
   - Import icons from lucide-react: Github, Twitter (or use X if available, otherwise Twitter icon)
   - Accept optional className prop for positioning flexibility
   - Define links array:
     - { href: "https://github.com/PLACEHOLDER", icon: Github, label: "GitHub" }
     - { href: "https://x.com/PLACEHOLDER", icon: Twitter, label: "X / Twitter" }
   - Render as flex column (or row) of links:
     - Each link: flex items-center gap-2
     - Icon: h-5 w-5 text-zinc-400
     - Label: text-sm text-zinc-400 hover:text-zinc-200 transition-colors
     - target="_blank" rel="noopener noreferrer" for external links
   - Use PLACEHOLDER for URLs - user will fill in real values

2. Create `app/components/about/Bio.tsx`:
   - Server Component
   - Import Image from next/image (for optimized avatar)
   - Accept optional className prop
   - Structure:
     - Flex layout: flex-col md:flex-row gap-8 items-start
     - Avatar section:
       - Placeholder div (rounded-full bg-zinc-800 w-32 h-32) OR
       - Image component if avatar file exists
       - Add border-2 border-zinc-700 for definition
     - Text section (flex-1):
       - Name heading: text-2xl font-bold text-zinc-100 mb-4
       - Long-form bio: multiple paragraphs of text-zinc-400 leading-relaxed
       - Use prose-like styling: max-w-prose, space-y-4 between paragraphs
   - Use PLACEHOLDER text for bio content - this will be personalized by user
   - Placeholder bio should indicate what goes there (journey, philosophy, what drives you per CONTEXT.md)
  </action>
  <verify>
    - Both files exist
    - SocialLinks renders GitHub and X/Twitter links
    - Bio renders avatar placeholder and text
  </verify>
  <done>SocialLinks and Bio components created with placeholder content</done>
</task>

<task type="auto">
  <name>Task 2: Assemble About page</name>
  <files>
    app/about/page.tsx
  </files>
  <action>
1. Replace `app/about/page.tsx` placeholder with full implementation:
   - Import Bio and SocialLinks components
   - Export metadata: title "About | agentstype.dev", description about the developer
   - Server Component (no "use client")
   - Page structure:
     - Main wrapper: min-h-screen bg-background pt-16 pb-24 (account for nav)
     - Content container: max-w-4xl mx-auto px-4
     - Page title: "About" - text-3xl font-bold text-zinc-100 mb-8
     - Two-column layout on desktop: grid grid-cols-1 lg:grid-cols-[1fr_200px] gap-8
       - Left: Bio component (full bio text with avatar)
       - Right (sidebar): SocialLinks component (sticky lg:sticky lg:top-24)
     - On mobile: Social links appear after bio (single column)

Note: CONTEXT.md says "Social links in sidebar/aside alongside bio" - this grid layout achieves that.
  </action>
  <verify>
    - File exists at app/about/page.tsx (not just placeholder)
    - bun run build succeeds
    - Visit http://localhost:3000/about - see full page layout
  </verify>
  <done>About page assembled with bio and social links</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Pages & Navigation phase:
    - Navigation component (desktop header + mobile bottom nav)
    - Home page (hero, terminal preview, feature cards)
    - About page (bio, social links)
    - All pages mobile responsive
  </what-built>
  <how-to-verify>
    1. Start dev server: bun dev
    2. Desktop verification (width > 768px):
       - Visit http://localhost:3000 (Home)
       - Verify: Header nav at top, hero with tagline, terminal preview, CTA button, feature cards
       - Click "Enter the Stream" or terminal - goes to /live
       - Live page has both site nav AND feed header with expand/collapse
       - Click "About" in nav - goes to /about
       - About page shows avatar placeholder, bio text, social links in sidebar
       - Social links open in new tab
    3. Mobile verification (resize browser or use DevTools):
       - Bottom nav visible with icons (Home, Live, About)
       - Header nav hidden
       - Home: terminal above text, cards stacked
       - About: single column, social links below bio
       - Tap navigation items - all work
    4. Navigation state:
       - Active page highlighted in nav
       - No flash on navigation (layout persists)
    5. Content placeholders to fill:
       - Bio text needs personalization
       - Social links URLs need real values
       - Consider adding actual avatar image
  </how-to-verify>
  <resume-signal>Type "approved" if all navigation and pages work, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `bun run build` completes without errors
2. All 3 pages accessible: /, /live, /about
3. Navigation works on all pages (desktop and mobile)
4. About page requirements met:
   - ABOUT-01: Full bio text visible
   - ABOUT-02: GitHub link visible and clickable
   - ABOUT-03: X/Twitter link visible and clickable
5. Mobile responsive (DESGN-06):
   - All pages render correctly on mobile viewport
   - Bottom nav functional
   - Content doesn't overflow
</verification>

<success_criteria>
- About page displays bio with photo/avatar placeholder
- Social links present and functional (external links)
- Page is responsive (single column on mobile, sidebar on desktop)
- Navigation highlights About as active when on /about
- Human verification confirms all pages work correctly
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-pages-navigation/05-03-SUMMARY.md`
</output>
