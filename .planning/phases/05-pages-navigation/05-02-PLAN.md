---
phase: 05-pages-navigation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/page.tsx
  - app/components/ui/TerminalMockup.tsx
  - app/components/home/Hero.tsx
  - app/components/home/FeatureCards.tsx
  - app/components/home/LivePreview.tsx
autonomous: true

must_haves:
  truths:
    - "Home page shows hero with tagline and CTA button"
    - "Hero shows terminal mockup with live event preview"
    - "Terminal mockup shows LIVE badge when session active"
    - "Clicking terminal or CTA navigates to /live"
    - "Feature cards appear below hero fold"
    - "Home page is responsive on mobile"
  artifacts:
    - path: "app/page.tsx"
      provides: "Home page with hero and features"
      min_lines: 20
    - path: "app/components/ui/TerminalMockup.tsx"
      provides: "macOS-style terminal window component"
      exports: ["TerminalMockup"]
      min_lines: 25
    - path: "app/components/home/Hero.tsx"
      provides: "Hero section with tagline, bio intro, CTA"
      exports: ["Hero"]
      min_lines: 40
    - path: "app/components/home/FeatureCards.tsx"
      provides: "3 feature highlight cards"
      exports: ["FeatureCards"]
      min_lines: 30
    - path: "app/components/home/LivePreview.tsx"
      provides: "Real-time event preview for terminal"
      exports: ["LivePreview"]
      min_lines: 30
  key_links:
    - from: "app/page.tsx"
      to: "app/components/home/Hero.tsx"
      via: "import and render"
      pattern: "import.*Hero"
    - from: "app/components/home/LivePreview.tsx"
      to: "convex/events"
      via: "useQuery for latest events"
      pattern: "useQuery.*listEvents"
---

<objective>
Create the Home page with hero section, terminal mockup showing live events, and feature highlight cards.

Purpose: Entry point for site visitors - showcases what agentstype.dev is about and invites them to watch live coding.
Output: Full Home page with real-time preview of latest events.
</objective>

<execution_context>
@/Users/alexander.buss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexander.buss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-pages-navigation/05-CONTEXT.md
@.planning/phases/05-pages-navigation/05-RESEARCH.md
@.planning/phases/05-pages-navigation/05-01-SUMMARY.md

# Existing query to reuse
@convex/events.ts

# Reference for event display patterns
@app/components/feed/EventCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TerminalMockup and LivePreview components</name>
  <files>
    app/components/ui/TerminalMockup.tsx
    app/components/home/LivePreview.tsx
  </files>
  <action>
1. Create `app/components/ui/TerminalMockup.tsx`:
   - Accept children and optional title prop (default "Terminal")
   - macOS-style window with traffic light buttons (red, yellow, green circles)
   - Title bar: flex items-center gap-2, px-4 py-3, bg-zinc-800/50, border-b border-zinc-700
   - Traffic lights: div with flex gap-2, each dot is h-3 w-3 rounded-full
   - Red: bg-red-500, Yellow: bg-yellow-500, Green: bg-green-500
   - Title: flex-1 text-center text-xs text-zinc-500
   - Outer wrapper: rounded-lg border border-zinc-800 bg-zinc-900 overflow-hidden shadow-2xl
   - Content area: p-4 font-mono text-sm text-zinc-300
   - Accept optional className for outer wrapper
   - Pure presentational component (no "use client" needed if only rendering children)

2. Create `app/components/home/LivePreview.tsx`:
   - "use client" directive (uses Convex query)
   - Import useQuery from convex/react, api from @/convex/_generated/api
   - Query latest 3 events: useQuery(api.events.listEvents, { limit: 3 })
   - Determine if live: check if any event timestamp is within 5 minutes of now
   - Show "LIVE" badge overlay when live (absolute positioned, top-right of terminal)
   - Badge: animate-pulse bg-red-600 text-white text-xs px-2 py-0.5 rounded
   - Display events in simplified format:
     - Tool type icon/indicator (colored square or dot matching EventCard colors)
     - Brief action description (use same logic as EventCard for summary)
     - Relative timestamp (use date-fns formatDistanceToNow)
   - If no events: show placeholder text "Waiting for coding session..."
   - Wrap in Link to="/live" so entire preview is clickable
   - Add hover:opacity-90 transition for click affordance

Use existing event type color scheme from EventCard.tsx for consistency.
  </action>
  <verify>
    - Both files exist
    - TerminalMockup renders traffic lights and content
    - LivePreview queries events and shows them
  </verify>
  <done>TerminalMockup and LivePreview components created</done>
</task>

<task type="auto">
  <name>Task 2: Create Hero and FeatureCards components</name>
  <files>
    app/components/home/Hero.tsx
    app/components/home/FeatureCards.tsx
  </files>
  <action>
1. Create `app/components/home/Hero.tsx`:
   - "use client" directive (contains LivePreview which is client)
   - Import TerminalMockup, LivePreview, Link from next/link, Radio from lucide-react
   - Split layout: grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-center
   - Left column (text content):
     - Tagline: text-4xl lg:text-5xl font-bold text-zinc-100, something like "Watch Code Come Alive"
     - Subheader: text-lg text-zinc-400 mt-4, explaining what the site is (real-time Claude Code sessions)
     - Bio intro: text-sm text-zinc-500 mt-6, 2-3 sentences about who you are (placeholder text for now)
     - CTA button: Link to="/live", styled as primary button
       - Inline-flex items-center gap-2
       - bg-emerald-600 hover:bg-emerald-500 text-white
       - px-6 py-3 rounded-lg font-medium
       - transition-colors
       - Include Radio icon (size 18) before text "Enter the Stream"
   - Right column (terminal preview):
     - TerminalMockup wrapping LivePreview
     - On mobile: order-first to show terminal above text
   - Add responsive padding: px-4 py-8 lg:py-16

2. Create `app/components/home/FeatureCards.tsx`:
   - Server Component (no "use client" needed - pure static content)
   - Import icons from lucide-react: Zap, Clock, GitBranch (or similar for the 3 features)
   - Define features array with 3 items per CONTEXT.md:
     - Real-time Events: "Watch file reads, edits, and commands as they happen"
     - Session Context: "See what I'm building, how long it takes, what gets committed"
     - Agent Depth: "Peek under the hood when Claude spawns sub-agents for complex tasks"
   - Grid layout: grid grid-cols-1 md:grid-cols-3 gap-6
   - Each card:
     - p-6 rounded-lg border border-zinc-800 bg-zinc-900/50
     - Icon: h-8 w-8 text-emerald-500 mb-4
     - Title: text-lg font-semibold text-zinc-100 mb-2
     - Description: text-sm text-zinc-400
   - Section wrapper: px-4 py-12 max-w-5xl mx-auto
  </action>
  <verify>
    - Both files exist
    - Hero has split layout with tagline and terminal
    - FeatureCards shows 3 cards in grid
  </verify>
  <done>Hero and FeatureCards components created</done>
</task>

<task type="auto">
  <name>Task 3: Assemble Home page and verify integration</name>
  <files>
    app/page.tsx
  </files>
  <action>
1. Replace `app/page.tsx` content:
   - Remove the redirect import and redirect call
   - Import Hero and FeatureCards components
   - Export metadata: title "agentstype.dev", description about watching Claude Code sessions
   - Render structure:
     - Main wrapper: min-h-screen bg-background
     - Add padding-top for navigation: pt-16 (desktop header height)
     - Hero section: max-w-6xl mx-auto
     - Divider: optional subtle border-t border-zinc-800/50
     - FeatureCards section
   - Page is Server Component by default (Hero is client component that will render client-side)

Note: The old page.tsx just redirected to /live. We're replacing that with the actual home page content.
  </action>
  <verify>
    - bun run build succeeds
    - Visit http://localhost:3000 - see home page (NOT redirect to /live)
    - Hero shows with tagline, terminal preview, CTA
    - Feature cards appear below
    - Click CTA or terminal - navigates to /live
    - Terminal shows LIVE badge if events are recent
    - Mobile: content stacks vertically, terminal on top
  </verify>
  <done>Home page assembled with all components, redirect removed</done>
</task>

</tasks>

<verification>
1. `bun run build` completes without errors
2. Start dev server: `bun dev`
3. Home page content verification:
   - Visit http://localhost:3000
   - See tagline and subheader
   - See bio intro paragraph
   - See "Enter the Stream" CTA button with icon
   - Terminal mockup with traffic lights visible
   - Live events displayed in terminal (or "Waiting..." if no recent events)
4. Feature cards verification:
   - Scroll down to see 3 feature cards
   - Icons, titles, descriptions all present
   - Grid layout on desktop, stack on mobile
5. Navigation integration:
   - Click "Enter the Stream" - goes to /live
   - Click terminal preview - goes to /live
   - Navigation bar shows "Home" as active
6. Responsive verification:
   - Resize to mobile width
   - Terminal appears above text
   - Cards stack vertically
   - Bottom nav visible instead of header
</verification>

<success_criteria>
- Home page displays hero with all required elements (HOME-01, HOME-02)
- Terminal shows live indicator when session active (HOME-03)
- Latest events preview in terminal (HOME-04)
- Feature cards below fold
- Clicking preview/CTA navigates to /live
- Mobile responsive layout (DESGN-06 partial)
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-pages-navigation/05-02-SUMMARY.md`
</output>
